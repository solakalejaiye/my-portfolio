import { notFound } from "next/navigation";
import { caseStudies } from "../../case-data";
import { Contact,SiteHeader } from "../../components";
const published=caseStudies.filter(s=>s.slug!=="conversational-ai");
export function generateStaticParams(){return published.map(({slug})=>({slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const s=published.find(x=>x.slug===slug);return{title:s?s.title+" — Olusola Kalejaiye":"Case Study"}}
export default async function CasePage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const s=published.find(x=>x.slug===slug);if(!s)notFound();return <main><SiteHeader/><article className="casePage"><header className="caseHero shell"><a className="backLink" href="/cases">← All cases</a><p className="kicker">{s.client}</p><h1>{s.title}</h1><p className="caseSubtitle">{s.subtitle}</p><div className="caseFacts"><div><span>Year</span><strong>{s.year}</strong></div><div><span>Role</span><strong>{s.role}</strong></div><div><span>Timeline</span><strong>{s.timeline}</strong></div><div><span>Focus</span><strong>{s.tags}</strong></div></div></header><div className={"caseCover shell "+(s.client==="Interswitch"||s.client==="Heroshe"?"landscape":"portrait")}><img src={s.hero} alt={s.client+" project"}/></div>{s.metrics&&<section className="metrics shell">{s.metrics.map(m=><div key={m.label}><strong>{m.value}</strong><span>{m.label}</span></div>)}</section>}<div className="caseBody shell">{s.sections.map((section,i)=><section className="caseSection" key={section.title}><div><span>0{i+1}</span><h2>{section.title}</h2></div><div><p>{section.body}</p>{section.image&&<img src={section.image} alt={section.title}/>}</div></section>)}</div></article><Contact/></main>}
